// TODO: Implement Chat Screen with Gemini AI
import 'package:flutter/material.dart';

class ChatScreen extends StatelessWidget {
  const ChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement chat UI with Gemini integration
    return Scaffold(
      appBar: AppBar(title: const Text('Chat with Kado')),
      body: const Center(
        child: Text('💬 Chat Screen - TODO'),
      ),
    );
  }
}
