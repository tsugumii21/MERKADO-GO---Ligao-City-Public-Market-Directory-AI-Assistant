// TODO: Implement Map Screen with Google Maps
import 'package:flutter/material.dart';

class MapScreen extends StatelessWidget {
  const MapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement Google Maps with stall markers
    return Scaffold(
      appBar: AppBar(title: const Text('Merkado Go')),
      body: const Center(
        child: Text('🗺️ Map Screen - TODO'),
      ),
    );
  }
}
