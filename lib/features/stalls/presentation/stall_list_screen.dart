// TODO: Implement Stall List Screen
import 'package:flutter/material.dart';

class StallListScreen extends StatelessWidget {
  const StallListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement stall list UI with categories
    return Scaffold(
      appBar: AppBar(title: const Text('Stalls')),
      body: const Center(
        child: Text('🏪 Stall List Screen - TODO'),
      ),
    );
  }
}
